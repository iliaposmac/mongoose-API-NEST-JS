<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Title</title>
</head>
<body>
    {{#if users}}
        <p>{{users.name}}</p>
        <p>{{users.age}}</p>
        <p>{{users._id}}</p>
    {{/if}}
</body>
</html>